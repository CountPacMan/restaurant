<!DOCTYPE html>
<html>
  <head>
    <link rel='stylesheet' href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/css/bootstrap.min.css">
    <title>Restaurant Jamboreeeeee!!!</title>
  </head>
  <body>

    <div class='container'>
      <h1>Welcome to Restaurant Jamboreeeeee!!!!!</h1>

      <p>Create a new cuisine type!!</p>

      <form action='/cuisines' method='post'>
        <div class='form-group'>
          <label for='type'>Cuisine type:</label>
          <input id='type' name='type' class='form-control' type='text'>
        </div>
        <button type='submit' class='btn btn-success'>Submit</button>
      </form>

      {% if cuisines is not empty %}
        <p>Here are all your cuisines:</p>
        <ul>
          {% for cuisine in cuisines %}
            <li><a href="/cuisines/{{ cuisine.getId }}">{{ cuisine.getType }}</a></li>
          {% endfor %}
        </ul>
        {% else %}
          <p>There are no cuisines yet!</p>
      {% endif %}

      <form action='/search' method='post'>
        <div class='form-group'>
          <label for='name'>Restaurant to search:</label>
          <input id='name' name='name' class='form-control' type='text'>
        </div>
        <button type='submit' class='btn btn-success'>GIMME</button>
      </form>
    </div>

  </body>
</html>
